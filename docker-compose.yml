services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: jassas
      POSTGRES_PASSWORD: jassas
      POSTGRES_DB: assessment
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
